# Generated Gate 0 Reports

Run `python tools/audit_assets.py` from the repository root. Generated reports are ignored by Git because they are derived from the source ZIPs; GitHub Actions publishes them as the `gate-0-asset-audit` artifact.
