# Asset issues

Generated by `tools/build_assets.py` from the local ZIP contents. Local files outrank storefront descriptions.

- Pack `Forgotten Cemetery` ships no licence/terms text file.
- Pack `High Forest` ships no licence/terms text file.
- Player has no authored hurt/land/interact/heal frames in the collection. `hurt` is a derived flash stand-in (Section 5 permits this before final); Appendix B still lists these as required before release.
- biome `deadwind`: `Assets/Bones.aseprite` ships only as an Aseprite source; recovered to bones.png by tools/aseprite.py.
- biome `deep_cave`: the storefront advertises one unidentified enemy. There is no mob folder in the local ZIP -- the pack is environment-only. Chapter 10 uses slimes and bats-by-behaviour from other packs instead.
- biome `fortress`: `Tiles/city.aseprite` ships only as an Aseprite source; recovered to city.png by tools/aseprite.py.
- biome `mine`: confirmed the master plan's Appendix A warning, and it goes further than the storefront text. Lonely Mine's `Assets/Tiles.png` contains forest material -- dark foliage, ferns, vines, mushrooms and berries -- with no rails, timber supports, carts or lamps anywhere in the tileset. The pack's mining identity exists only in Background/Background.png and Background/ForeGround.png. Chapter 9 is therefore authored as an abandoned worksite being reclaimed by growth, with the machinery read from the background and foreground layers rather than from tiles.
- biome `monastery`: `Assets/Background.aseprite` ships only as an Aseprite source; recovered to bg0.png by tools/aseprite.py.
- biome `purple_bay`: the pack ships a complete crab mob (idle/walk/attack/death) that the master plan's Section 6 lists as *missing* art needing commission. It is asset-backed; the roster uses it.
- biome `swamp`: Muddy Swamp ships no background layer at all. Chapter 8 composites its parallax from the pack's own tree and canopy tiles plus a tinted flat, per the creator's soft-light note.
- biome `swamp`: `Assets/Tree.aseprite` ships only as an Aseprite source; recovered to tree.png by tools/aseprite.py.
