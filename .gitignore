# ---------------------------------------------------------------- toolchains
node_modules/
.venv/
__pycache__/
*.py[cod]

# Godot-generated files, from the master plan's recommended engine profile
.godot/
.import/
export.cfg
export_presets.cfg

# ------------------------------------------------------------ build outputs
# Vite build for the Phaser prototype at the repository root
dist/

# Extracted pack sources. The ZIPs in the repository root are the archive of
# record; build/_source is regenerated by tools/build_assets.py and must never
# be edited by hand.
build/

# Working folders used by the audit tooling
assets/_source/
assets/normalized/
builds/
captures/

# Local audit output (the Gate 0 workflow publishes this as an artifact)
docs/audit/

# ------------------------------------------------------- verification output
# Screenshots written by tools/verify.mjs. The reviewed copies that count as
# gate evidence are committed under docs/evidence/ instead.
shots/

# --------------------------------------------------- operating-system litter
.DS_Store
Thumbs.db
